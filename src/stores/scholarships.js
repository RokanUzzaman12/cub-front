import { atom } from 'nanostores';
export const scholarshipList = atom({});
export const singleScholarship = atom({});
export const scholarshipCategories = atom([]);
export const categoryWisescholarships = atom([]);
