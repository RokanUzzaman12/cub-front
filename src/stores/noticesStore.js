import { atom } from 'nanostores';
export const noticeList = atom([]);
export const singleNotice = atom({});
export const facultyWiseNotices = atom([]);
export const departmentWiseNotices = atom([]);
