import { atom } from 'nanostores';
export const staffList = atom([]);
export const singleStaff = atom({});
export const departmentWiseStaffs = atom([]);
export const facultyWiseStaffs = atom([]);
export const facultyCategoryWiseStaffs = atom({});
export const departmentCategoryWiseStaffs = atom({});
